# Copy to .env and uncomment what you need. Every value here is the default.

# CATPC_OLLAMA_HOST=http://127.0.0.1:11434
# CATPC_DEFAULT_MODEL=qwen3.5:9b

# Lower this if a model fails to load with cudaMalloc / out of memory.
# CATPC_NUM_CTX=8192

# CATPC_REQUEST_TIMEOUT=600
# CATPC_CONNECT_TIMEOUT=5
