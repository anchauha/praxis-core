[project]
name = "catpc-app"
version = "0.1.0"
description = "Standards-grounded lesson planning assistant over the Indiana Academic Standards index."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "httpx>=0.27",
    "pydantic-settings>=2.6",
]

[dependency-groups]
dev = []

[tool.uv]
package = false
